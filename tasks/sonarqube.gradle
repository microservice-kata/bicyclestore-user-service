
apply plugin: 'org.sonarqube'

sonarqube {
    properties {
        property "sonar.projectName", "bicyclestore-user-service"
        property "sonar.projectKey", "bicyclestore-user-service"
        property "sonar.jacoco.reportPaths", "${buildDir}/jacoco/junitPlatformTest.exec"
    }
}
